# 高标准软件工程与算法创新说明

## 1. 项目升级目标

本项目将“能演示的资源管理页面”升级为一个可解释、可审计、可实验验证的实验室算力治理 MVP。设计遵循四项原则：

1. **后端权威**：调度结果由后端计算，前端只负责解释和呈现，避免客户端篡改排序。
2. **公平且防饥饿**：同时考虑资源公平、等待时间和任务成本，避免只按紧急度造成长期等待。
3. **不确定性诚实**：训练时长给出点估计与区间，不把启发式预测包装成精确机器学习结论。
4. **全过程可追溯**：资源、轮转和评价变更形成结构化审计事件。

新申请采用固定 85 分的中性信用先验，确保实验可复现；后续接入真实释放记录后再更新信用，而不是使用随机数制造“个性化”假象。

## 2. XFS-V1 可解释公平调度器

XFS-V1（Explainable Fair Scheduler V1）借鉴多目标优化、Priority Aging、Dominant Resource Fairness 和可解释 AI 的思想。申请分数为：

```text
Score = U + C + A + E + F

U = 35 × urgency / 5                         紧急度
C = 20 × credit / 100                        信用
A = 15 × min(waitMinutes / 720, 1)           等待老化
E = 15 × (1 - min(GPU × hours / 1344, 1))    资源效率
F = 15 × (1 - activeGPU / totalGPU)          公平补偿
```

权重总和为 100。每个分量直接返回给前端，因此任何排名都能解释。等待老化项保证长时间等待的申请持续获得优先级，公平项会降低已经占用较多资源的学生的新增优势。

节点匹配同样采用多目标得分：

```text
NodeScore = 45% 空闲容量 + 30% 显存余量 + 15% 最佳适配 + 10% 数据源可信度
```

“最佳适配”减少分配后的资源碎片；真实 `nvidia-smi` 数据比演示种子数据获得更高可信度。检测到真实 GPU 时，只允许真实节点进入调度候选。

## 3. 公平性指标

系统实时计算 Jain Fairness Index：

```text
J = (Σxi)² / (n × Σxi²)
```

其中 `xi` 是每位申请者当前占用的 GPU 数。`J` 越接近 1，资源分配越均衡。该指标与调度排名同时展示，可用于对比不同权重配置，而不是仅凭主观感觉判断“公平”。

## 4. 不确定性感知预测

训练时长保留轻量先验缩放模型，但输出由单点值升级为：

- 点估计；
- 上下界；
- 置信度等级；
- 当前模型方法说明。

在真实历史数据不足时，这比伪造高精度回归模型更严谨。后续采集足够任务数据后，可将该接口替换为分位数回归、Conformal Prediction 或贝叶斯回归，并用覆盖率验证区间是否校准。

## 5. 审计与安全

`audit_events` 是应用层 append-only 事件表，记录申请、分配、驳回、暂停、恢复、快照、释放、进度和评分。事件包含实体、动作、执行者、结构化上下文与时间，用于：

- 复盘一次调度为什么发生；
- 对照 Docker 状态与数据库状态；
- 统计资源治理指标；
- 为未来身份认证和责任追踪保留接口。

静态资源响应增加 CSP、`nosniff`、Referrer Policy；Docker GPU 参数从 `--gpus all` 改为申请数量，数据集继续使用只读挂载。

## 6. 可复现实验设计

答辩或报告建议使用固定申请序列，对比旧规则与 XFS-V1：

| 指标 | 定义 | 目标 |
| --- | --- | --- |
| 平均等待时间 | 从提交到分配的平均分钟数 | 不显著劣于旧规则 |
| P95 等待时间 | 最慢 5% 申请的等待时间 | 明显降低 |
| Jain 指数 | 学生 GPU 占用公平性 | 提升并接近 1 |
| 饥饿率 | 超过阈值仍未分配的申请占比 | 接近 0 |
| GPU 利用率 | 已使用 GPU / 总 GPU | 保持或提高 |
| 决策可解释率 | 具备完整分量和节点理由的决策占比 | 100% |

实验应固定输入、算法版本和权重，至少重复 30 次；报告均值、标准差和效应量。若样本不服从正态分布，使用 Mann–Whitney U 检验而非机械使用 t 检验。

## 7. 团队协作建议

建议四人小组按职责而非页面拆分：

| 角色 | 主要责任 | 验收产物 |
| --- | --- | --- |
| 架构与后端 | API、事务、SQLite、审计 | 服务层测试、架构决策记录 |
| 算法与实验 | XFS 权重、基线、统计分析 | 可复现实验脚本、结果表 |
| 前端与交互 | 决策解释、无障碍、响应式 | 桌面/移动验收、交互测试 |
| DevOps 与质量 | Docker、CI、发布、安全 | 一键验证、部署记录、风险清单 |

所有变更通过功能分支和 Pull Request 合并；每个 PR 必须包含问题描述、设计取舍、测试证据和回滚方式。算法权重变化必须更新算法版本，禁止“静默调参”。

## 8. 严谨边界

- XFS-V1 是透明的多目标启发式调度器，不宣称为机器学习或全局最优解。
- 当前公平性按 GPU 数衡量，尚未纳入 GPU 型号异构和实际运行时。
- 预测区间是先验不确定性区间，尚未经过真实数据的覆盖率校准。
- 审计表在应用层保持 append-only，生产环境还需数据库权限和签名链保证防篡改。
- 完整生产化仍需要身份认证、RBAC、并发锁、任务队列和 Kubernetes 调度。
